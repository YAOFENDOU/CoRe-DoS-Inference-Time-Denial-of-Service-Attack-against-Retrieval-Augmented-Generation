# Core dependencies
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0

# For RAGuard evaluation
# (same as above — no additional packages needed)

# Optional: for API-based LLMs (transfer experiments)
# openai>=1.0.0
# anthropic>=0.20.0
# google-generativeai>=0.4.0
